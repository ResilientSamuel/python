def calculate(op1, op2, f):
	if f == "add":
	   return op1 + op2
	elif f == "subtract":
		return op1 - op2
	elif f == "division":
		return op1 / op2
	elif f == "multiply"
		return op1 * op2
		
value = calculate(4,5, "add")
print value
value = calculate(18,6, "subtract")
print value
		
